# Ralph Progress Log
Started: Fri Apr 24 09:26:03 CEST 2026

## Codebase Patterns
- Config dataclasses follow the pattern: define dataclass, create `_build_X(data: dict)` helper, call helper in `load_config()` with `data.get("section", {}) or {}`
- Python binary is `python3` (not `python`) on this system
- No mypy installed — use `python3 -m py_compile` for typecheck
- Config YAML sidecars are loaded from `{input_stem}.yaml` next to the markdown file

---

## 2026-04-24 - US-001
- Added `AudioConfig` dataclass with fields: voice, rate, volume, pitch, bitrate, chapter_pause, concurrency, intro, outro
- Added `audio: AudioConfig` field to `BookConfig` with `field(default_factory=AudioConfig)`
- Added `_build_audio()` helper following the same pattern as `_build_cover()`
- Integrated audio parsing in `load_config()` — reads `audio:` section from YAML sidecar
- Files changed: `epub_generator/config.py`
- **Learnings for future iterations:**
  - The config module follows a consistent pattern: dataclass + `_build_X` helper + integration in `load_config()`
  - Default values chosen for TTS: rate=-5% (slightly slower for clarity), bitrate=96k (good quality for speech)
---

## 2026-04-24 - US-002
- Added ffmpeg/ffprobe validation at start of `convert_audio()` using `shutil.which()` — raises `RuntimeError` with install instructions
- Added ffmpeg/ffprobe warning check in `generate_epub.sh` (section 3, between ebook-convert and fonts)
- Removed conditional `shutil.which("ffmpeg")` guard from `_assemble_m4b` (no longer needed)
- Files changed: `epub_generator/converters/audio.py`, `generate_epub.sh`
- **Learnings for future iterations:**
  - The shell script uses `detect_install_cmd()` helper for cross-platform install instructions — use it for any new dependency checks
  - The ffmpeg check in the shell script is a warning (not exit 1) since ffmpeg is only needed for `--format audio`, not for epub generation
---

## 2026-04-24 - US-003
- Added `strip_frontmatter(text: str) -> str` function that removes YAML frontmatter (`---\n...\n---`) from the beginning of text using `re.sub` with `\A` anchor and `re.DOTALL`
- Called `strip_frontmatter()` at the start of `extract_chapters()` before heading search
- Horizontal rules (`---`) mid-document are preserved because the regex requires `\A` (start of string)
- Files changed: `epub_generator/preprocessors/markdown_cleaner.py`
- **Learnings for future iterations:**
  - `markdown_cleaner.py` is the central place for all text preprocessing before TTS — both chapter extraction and text cleaning
  - Use `\A` anchor (not `^`) to match only the absolute start of string, even with `re.DOTALL`
---

## 2026-04-24 - US-004
- Added emoji removal in `clean_for_tts()` using regex character class with Unicode ranges: emoticons, symbols, pictographs, transport, flags, dingbats, variation selectors, supplemental symbols, and zero-width joiner
- Added decorative arrow removal (U+2190-U+21FF)
- Steps placed after HTML tag removal and before blockquote cleaning
- Hispanic characters (tildes, ñ, ¿, ¡, etc.) are preserved — verified with test
- Files changed: `epub_generator/preprocessors/markdown_cleaner.py`
- **Learnings for future iterations:**
  - Python supports Unicode escapes directly in regex character classes (e.g., `"\U0001F600-\U0001F64F"`) — no need for external emoji libraries
  - The cleaning pipeline order matters: emoji removal should go after HTML tags (which might contain encoded emojis) and before collapsing blank lines
---

## 2026-04-24 - US-005
- Added bare URL removal step in `clean_for_tts()` using regex `https?://[^\s)]+`
- Placed AFTER markdown link processing (which already extracts link text and drops URLs), so only truly bare URLs are matched
- URLs inside code blocks are already replaced with CODE_REDIRECT before this step runs
- Files changed: `epub_generator/preprocessors/markdown_cleaner.py`
- **Learnings for future iterations:**
  - The order of cleaning steps in `clean_for_tts()` is critical: markdown links must be processed first so their URLs don't get caught by the bare URL regex
  - Code blocks are replaced early in the pipeline, so their content (including URLs) is already gone by the time later steps run
---

## 2026-04-24 - US-006
- Refactored `_chapter_to_mp3` to async `_chapter_to_mp3_async` with semaphore-based concurrency control
- Created `_synthesize_all()` that uses `asyncio.Semaphore(concurrency)` and `asyncio.gather()` to narrate chapters in parallel
- Reads concurrency from `config.audio.concurrency` (default 5)
- Replaced sequential loop in `convert_audio()` with single `asyncio.run(_synthesize_all(...))`
- Each chapter logs progress as "Narrando capítulo X de N: {title}"
- Failed chapters are caught with try/except, logged with `log.error`, and return None — other chapters continue
- Files changed: `epub_generator/converters/audio.py`
- **Learnings for future iterations:**
  - `asyncio.gather()` preserves order of results matching input order, so mp3_files list stays correctly ordered even with parallel execution
  - `asyncio.Semaphore` inside `async with` is the clean way to limit concurrency — no need for manual task batching
  - `clean_for_tts()` is called inside each async task (not pre-computed) since it's CPU-bound and fast — no need to optimize separately
---

## 2026-04-24 - US-007
- Read `config.audio.voice`, `rate`, `volume`, `pitch` and pass them through the call chain: `convert_audio` -> `_synthesize_all` -> `_chapter_to_mp3_async` -> `_synthesize` -> `edge_tts.Communicate()`
- If `config.audio.voice` is empty string (default), falls back to language-based voice mapping via `_voice_for()`
- Removed redundant `import re` from inside `_slugify()` — moved to top-level imports
- Files changed: `epub_generator/converters/audio.py`
- **Learnings for future iterations:**
  - `edge_tts.Communicate()` accepts `rate`, `volume`, and `pitch` as keyword arguments — these are string values like "-5%", "+0%", "+0Hz"
  - The TTS parameter chain flows: `convert_audio` reads config -> `_synthesize_all` -> `_chapter_to_mp3_async` -> `_synthesize` -> `edge_tts.Communicate`
---

## 2026-04-24 - US-008
- Created `_get_duration_ms(mp3_path)` that uses `ffprobe -v error -show_entries format=duration -of csv=p=0` to get duration in ms
- Created `_generate_chapter_metadata(mp3_files, chapter_titles, pause_ms)` that generates ffmpeg metadata with `[CHAPTER]` entries using `TIMEBASE=1/1000`
- Updated `_assemble_m4b` to accept `chapter_titles`, generate metadata file, and pass `-i metadata.txt -map_metadata 1` to ffmpeg
- Chapter timestamps account for pauses between chapters (pause_ms from `config.audio.chapter_pause * 1000`)
- Metadata and concat temp files are cleaned up after assembly
- Updated `convert_audio()` to build `chapter_titles` list in sync with `mp3_files` (skipping failed chapters)
- Files changed: `epub_generator/converters/audio.py`
- **Learnings for future iterations:**
  - ffmpeg metadata format requires `;FFMETADATA1` header, then `[CHAPTER]` sections with `TIMEBASE`, `START`, `END`, `title`
  - `-map_metadata 1` tells ffmpeg to use the second input (index 1) as metadata source — the first input (index 0) is the concat audio
  - When filtering None results from parallel synthesis, must keep chapter titles in sync with mp3_files — use `zip(results, chapters)` to pair them
---
